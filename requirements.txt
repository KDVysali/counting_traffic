# ---- core libs ----
Flask
flask-cors
gunicorn
requests
opencv-python-headless
pandas                  # ultralytics needs it

# ---- CPU-only PyTorch stack (latest) ----
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.7.1+cpu
torchvision==0.22.1+cpu
torchaudio==2.7.1+cpu   # small, optional

# ---- Ultralytics (after torch so it keeps these wheels) ----
ultralytics==8.3.159
