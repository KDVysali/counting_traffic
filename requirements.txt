# ---------- Regular libraries ----------
Flask
flask-cors
gunicorn
requests
opencv-python-headless
pandas          # (ultralytics needs it)

# ---------- Force CPU-only PyTorch stack ----------
# 1) Tell pip to also search the CPU wheel index
--extra-index-url https://download.pytorch.org/whl/cpu

# 2) Pin versions that exist in that CPU index
torch==2.2.2+cpu
torchvision==0.17.2+cpu
torchaudio==2.2.2+cpu   # optional but small; ensures no nvidia deps

# ---------- Ultralytics (after torch) ----------
ultralytics==8.3.159
